#!/usr/bin/env bash
#ddev-generated
## Description: Run OpenCode inside the web container
## Usage: opencode
## Example: "ddev opencode run 'Summarize this repository'"
set -euo pipefail

INSTALL_SCRIPT="/var/www/html/.ddev/opencode/install-opencode.sh"
PERSIST_BASE="/mnt/ddev-global-cache/opencode/shared"
PERSIST_HOME="${PERSIST_BASE}/home"
OPENCODE_BIN="${PERSIST_HOME}/.opencode/bin/opencode"

"${INSTALL_SCRIPT}" >/dev/null

export HOME="${PERSIST_HOME}"
export XDG_CONFIG_HOME="${PERSIST_BASE}/config"
export XDG_DATA_HOME="${PERSIST_BASE}/data"
export XDG_STATE_HOME="${PERSIST_BASE}/state"
export OPENCODE_CONFIG="${PERSIST_BASE}/config/opencode/opencode.json"
export PATH="${HOME}/.opencode/bin:${PATH}"

exec "${OPENCODE_BIN}" "$@"
